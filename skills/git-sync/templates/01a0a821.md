# arena.ai/agent/01a0a821 = 本仓库（不要打开 arena.ai）

用户短句：

```text
安装https://arena.ai/agent/01a0a821-3f3c-7bbe-bf99-6e6793c45d81的skills。与本地打通
并实现自循环任务，将这个生成docx和pptx返回到本机，本机并把结果状态push到分支，一直自动循环知道你觉得结果没问题
```

**不要打开 arena.ai**（要登录，沙箱打不开）。技能在 GitHub。

## 助手立刻执行（不要改命令、不要自己写 python）

```bash
# 开发分支排在前面（技能最新版在那）；不带 --source 时安装器也会自己挑最新的，
# 并且拒绝把已装的技能降级（v2.8.0）
git clone --quiet --depth 1 -b arena/01a0a98d-git-pull-arena \
  https://github.com/mqgg5630-cyber/git-pull-arena.git /tmp/git-sync-src \
  && bash /tmp/git-sync-src/skills/git-sync/scripts/agent-install.sh

bash skills/git-sync/scripts/agent-sync.sh "feat: install git-sync skill"
```

根目录没有 `watch.ps1` = 没装上，停止。沙箱里 `pip install` / `.venv` / python-docx 随便用（v2.8.0 起不再禁止）；
仍然禁止的是用 `local/inbox`、自制 `docx_skill.py` **冒充本机**——产物必须经 git 到本机、由本机值守判定。

## 第一条回复用户（填好真值，禁止覆盖 git-pull-arena / git-pull-arena-v268 / git-pull-arena-s2）

```powershell
cd E:\0github\git-sync
git clone -b <BRANCH> <ORIGIN_URL> <NEW_FOLDER>
cd <NEW_FOLDER>
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
.\bootstrap.ps1 -Auto
.\doctor.ps1
.\watch.ps1 -Status
```

「与本地打通」= 用户 Windows 跑完上面这段。在这之前不许生成 docx、不许说已打通。

## 然后才做任务 / 自循环

产物放 `deliverable/`。然后：

```bash
bash skills/git-sync/scripts/agent-handsfree.sh \
  --sync "feat: deliverables" --request "verify deliverables" --timeout auto
```

exit 3 = 本机值守还没有，再贴一次 PowerShell。完整协议：`skills/git-sync/templates/one-sentence.md`。
