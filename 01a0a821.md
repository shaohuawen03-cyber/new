# arena.ai/agent/… = 本仓库（不要打开 arena.ai）

用户短句（任意一条，助手自己展开）：

```text
安装https://arena.ai/agent/01a0a9f0-6097-7460-af9e-ead9486a0c0b的skills。与本地打通
```

```text
安装 arena/01a0a821-git-pull-arena skills, 与本地打通
```

```text
安装https://arena.ai/agent/01a0a821-3f3c-7bbe-bf99-6e6793c45d81的skills。与本地打通
并实现自循环任务，将这个生成docx和pptx返回到本机，本机并把结果状态push到分支，一直自动循环知道你觉得结果没问题
```

**映射规则**：`arena.ai/agent/01a0axxx-...`（任何 ID）→ GitHub **`shaohuawen03-cyber/new`** 分支 **`arena/01a0ae7a-new`**（技能总部，v2.9.2 起）。
旧 `mqgg5630-cyber/git-pull-arena` 仍可作后备，安装器会探测全部候选并装最新版。
**不要打开 arena.ai**（要登录，沙箱打不开）。技能只在 GitHub。

**账号策略**：谁仓库的会话，就用谁账号（`shaohuawen03-*` 会话 ↔ `shaohuawen03-cyber`；`mqgg*` 会话 ↔ `mqgg5630-cyber`）。
贴给用户的本机命令块由 `agent-handoff.sh` 生成，里面已带 `auth.ps1 -Account <仓库主>`，不要手打。

| 会话 | 分支 | 技能版本 |
|---|---|---|
| **`01a0ae7a`（技能总部，今后都装这里）** | **`arena/01a0ae7a-new`** @ `shaohuawen03-cyber/new` | **v2.9.2** |
| `01a0aa00`（本会话） | `arena/01a0aa00-git-pull-arena` | **v2.8.1** |
| `01a0a9f0`（用户刚链接的那个，技能来源） | `arena/01a0a9f0-git-pull-arena` | v2.8.1 |
| `01a0a98d`（技能开发分支，v2.8.1 的出处） | `arena/01a0a98d-git-pull-arena` | v2.8.1 |
| `01a0a821`（短句约定页的旧总部） | `arena/01a0a821-git-pull-arena` | v2.7.4 |

## 助手立刻执行（不要改命令、不要自己写 python 冒充本机）

```bash
# 装最新的带技能的分支（不带 --source 时安装器也会自己挑最新版，并拒绝降级）
git clone --quiet --depth 1 -b arena/01a0ae7a-new \
  https://github.com/shaohuawen03-cyber/new.git /tmp/git-sync-src \
  && bash /tmp/git-sync-src/skills/git-sync/scripts/agent-install.sh

bash skills/git-sync/scripts/agent-sync.sh "feat: install git-sync skill"
```

根目录没有 `watch.ps1` = 没装上，停止。沙箱里 `pip install` / `.venv` / python-docx **随便用**（v2.8.0 起）；
仍然禁止的是用 `local/inbox`、自制 `docx_skill.py` **冒充本机**——产物必须经 git 到本机、由本机值守判定。

## 第一条回复用户（**由脚本生成，不要手打**）

```bash
bash skills/git-sync/scripts/agent-handoff.sh        # 直接打印填好的 PowerShell 块
bash skills/git-sync/scripts/agent-handoff.sh --json # 需要真值时
```

它自己读 `git remote get-url` + `sync.config.json` 的 `branch`；配置分支与 HEAD 不一致直接 exit 3 拒绝。
生成结果长这样（真值已替换；本会话文件夹 = `git-pull-arena-01a0aa00`）：

```powershell
cd E:\0github\git-sync
git clone -b arena/01a0ae7a-new https://github.com/shaohuawen03-cyber/new.git new-01a0ae7a
cd new-01a0ae7a
.\auth.ps1 -Account shaohuawen03-cyber    # policy: push as the REPO OWNER's account
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
.\bootstrap.ps1 -Auto
.\doctor.ps1
.\watch.ps1 -Status
```

禁止覆盖：`git-pull-arena` / `git-pull-arena-v268` / `git-pull-arena-s2` / `git-pull-arena-01a0a9f0`。
「与本地打通」= 用户 Windows 上跑完这段（值守注册好、心跳在走）。在那之前不许说已打通。

## 然后才做任务 / 自循环

产物放 `deliverable/`（本会话示例：`BRIDGE_01a0aa00_v2.8.1.docx/.pptx` + `OFFICE_HASHES.json`，
由 `code/make_bridge_report.py` 生成并先过同一套 3a–3g 的 Python 镜像自检）。然后：

```bash
bash skills/git-sync/scripts/agent-handsfree.sh \
  --sync "feat: deliverables" --request "verify round N" --timeout auto
```

exit 0 = 本机判定通过并已收尾；exit 2 = 看 `results/status/check_rN_*.txt` 再修一轮；
exit 3 = 本机值守还没起来，**停**，把 `agent-handoff.sh` 的块再贴一次，不要假装成功。
完整协议：`skills/git-sync/templates/one-sentence.md`、`task-loop.md`。
